# dsh-agy-provider 0.7.0 Release Candidate Checklist

状态：V7-M7 release commit 准备中；版本已 bump，CI/tag/publish/registry smoke 尚未完成。

Status: The V7-M7 release commit is being prepared. The version is bumped; CI, tag, publish, and registry smoke are still pending.

## RC baseline

- [x] Branch: `agent/v7-m4-capability-matrix`。
- [x] RC source HEAD before this checklist: `1aa6324`。
- [x] Published registry baseline remains `dsh-agy-provider@0.6.1` and `latest=0.6.1`。
- [x] No `v0.7.0` tag was created before the release commit。
- [x] No real AGY request was made during V7-M6; all public CI, fake adapter and local fixtures report `quotaUsed=false`。

## M6-1–M6-5 / M6-8 integration gates

- [x] Packed artifact inventory: 61 files, 79,166 bytes; no planning files, tests, fixtures, logs, environment files or credentials。
- [x] Web/headless DSH profile installation uses the packed artifact and native `dsh plugin add`。
- [x] DSH Session `cwd` is the workspace authority; Provider does not infer the project from `process.cwd()` or static configuration。
- [x] `read-only`, `workspace-write`, `danger-full-access`, read/write/edit/search, shell, local web, local MCP, approval and tool-result round-trip pass through DSH ToolRuntime。
- [x] Cross-platform packed-artifact matrix passed on Windows/Ubuntu/macOS × Node 22/24。
- [x] Provider verify matrix passed on Windows/Ubuntu/macOS × Node 20/22/24。
- [x] PR CI run [`32283700139`](https://github.com/darkings/dsh-agy-provider/actions/runs/32283700139) passed 17/17。
- [x] Local `npm run verify` passed typecheck, 141/141 tests and 61-file pack dry-run。
- [x] Local `npm run benchmark` passed; Windows Node 24 parser baseline was 893,927 events/s。

## M6-6 compatibility regression

- [x] Registry `dsh-agy-provider@0.6.1` Web/headless isolated install passed。
- [x] 0.6.1 legacy bundle was observed as `toolPolicy: agy-owned`; the smoke harness now makes the expected policy explicit instead of conflating it with the 0.7.0 RC default。
- [x] Registry no-tool Mock text response passed with `quotaUsed=false` and cleanup completed。
- [x] Current RC doctor v3, no-tool text, `UNSUPPORTED_TOOLS`, `PERMISSION_REQUIRED`, abort, timeout, retry, process-tree cleanup and tool-schema cleanup passed in 61/61 targeted tests。
- [x] `agy-owned` legacy warning remains read-only and recommends DSH-owned execution; no profile is rewritten automatically。

## M6-7 security and egress gates

- [x] Provider argv audit found no `--dangerously-skip-permissions` or equivalent automatic permission bypass。
- [x] Pack inventory and source audit found no npm/GitHub token, private key, refresh/access token assignment, user path, Prompt/response log or credential artifact。
- [x] Temporary tool schema and image staging files use mode `0600` and are removed on success and failure paths。
- [x] Public CI only installs from GitHub Actions/npm registry; HTTP and MCP fixtures bind to local `127.0.0.1`; CI never calls AGY or reads user profiles/configuration。

## V7-M7 release gates

- [x] Add `0.7.0` migration and synchronize Chinese/English README, installation, provider contract, capability matrix, compatibility matrix, changelog and this checklist。
- [x] Bump `package.json` and `package-lock.json` to `0.7.0` without unrelated dependency changes。
- [x] Run verify, benchmark, pack dry-run and self-contained source smoke after the version bump。
- [ ] Push the release commit and wait for the full PR CI to pass。
- [ ] Create and push the exact `v0.7.0` tag only after release CI is green。
- [ ] Confirm npm Trusted Publishing succeeds without storing or exposing a token。
- [ ] Verify npm `latest=0.7.0` and install `dsh-agy-provider@0.7.0` into empty Web/headless DSH profiles。
- [ ] Re-run the quota-free project/permission/Mock tool-bridge smoke from the registry artifact and record cleanup。

## Release blockers

Do not publish if any DSH permission mode, ToolRuntime bridge, doctor v3 safety check, packed install, cross-platform CI, tag/version check, Trusted Publishing run or registry smoke fails. Do not use a real AGY request to bypass a quota-free test failure.
