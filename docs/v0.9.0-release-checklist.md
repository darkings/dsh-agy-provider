# 0.9.0 发布检查单

本文是 0.9.0 从代码到 registry 的门禁清单，按 §8 的 7 层测试组织。

## 前置门禁

- [ ] `docs/v0.9.0-development-plan.md` 定义的 V9-M0~M4 范围、门禁、§7 预算、§10 发布边界已冻结
- [ ] Git 分支 `codex/v0.9.0-panel-workspace` 已合并至 `main` 预览，工作区干净

## L1 单元（无 DSH）— quotaUsed=false

- [ ] `npm run build` 通过
- [ ] `npm run typecheck` 通过
- [ ] `npm test` 或等价单测 160+ cases 通过（parser/serialize/limiter/models/visibleModels/normalize/i18n/dsh-context/doctor）
- [ ] `npm run benchmark` 无回归（parser >700k events/s）

## L2 Adapter 集成（fake 进程）— quotaUsed=false

- [ ] `visibleModels` 过滤：`visibleModels=["gemini-3.7-flash"]` 时 `listModels()` 仅返回 1，未勾选显式请求仍兼容
- [ ] 归一化：`parseAgyModels` 对 `agy models` 的 `-high` 后缀去重为 base，`mergeModelCatalog` 同
- [ ] 旧后缀兼容：`model: gemini-3.7-flash-high` 请求自动拆 base+high 且 warning
- [ ] workspace 无感：有/无 cwd 的文本/工具分支，`DshContextError` 可操作码
- [ ] transport 分发、tool 桥、abort/timeout/crash 均覆盖

## L3 自包含 Mock smoke — quotaUsed=false

- [ ] `npm run smoke:dsh:self-contained` 在隔离 `DSH_INSTALL_DIR` 通过
- [ ] 校验：`registerConfigurableProviders` 可见，Mock 文本流、`quotaUsed=false`、cleanup

## L4 权限矩阵 smoke — quotaUsed=false

- [ ] `npm run smoke:dsh:permission-matrix` 15+ 场景通过（read-only/workspace-write/danger-full-access × 文件/搜索/shell/web/MCP × 有/无 workspace）

## L5 设置面板 smoke（0.9.0 新增）— quotaUsed=false

- [ ] `npm run smoke:dsh:settings-panel` 通过：面板启用/模型勾选生效→选择器可见性、base+effort 下拉、workspace 无感文本/工具
- [ ] 中/英切换：分别以 `locale zh-CN / en` 启动 DSH Web，设置面板描述实时变更

## L6 跨平台 CI — quotaUsed=false

- [ ] GitHub Actions `verify`：Node 20/22/24 × Win/Ubuntu/macOS 全绿
- [ ] `dsh-smoke`：Node 22/24 × Win/Ubuntu/macOS 的自包含与权限矩阵全绿

## L7 真实 AGY 抽样（预算内）— 显式触发

- [ ] 预算内（见 development-plan §7：单次窗口 ≤2 请求，总硬上限 22/165k/12k）
- [ ] base+effort 实际 `--effort` 透传验证，旧后缀兼容实测
- [ ] 日志仅白名单字段，不含 Prompt/response/路径/凭据

## 发布动作

- [ ] `package.json` / `package-lock.json` bump 至 `0.9.0`，`CHANGELOG.md` 增 0.9.0 节，`cordis.patch.yml` 注释更新至 @0.9.0
- [ ] `npm run verify` + `benchmark` + `7 层` L1~L6 全绿
- [ ] `git tag v0.9.0` 指向 release commit，`git push origin main && git push origin v0.9.0`
- [ ] PR CI 17/17 通过，Trusted Publishing 成功，registry `latest=0.9.0` 可访问
- [ ] 发布后全新 Web/headless smoke：`plugin add dsh-agy-provider@0.9.0` + `doctor --json` 确认 `profileSchemaVersion:4`、`workspaceSource:dsh-session-cwd`、`visibleModels` 与 deprecation 警告

## 文档与宣发

- [ ] `README.md / README.en.md`、`docs/installation.md`、`docs/dsh-provider-contract.md`、`docs/compatibility-matrix.md` 已同步 0.9.0 面板/可见性/强度分离/工作区/i18n
- [ ] `docs/migration-0.9.0.md` 已发布
