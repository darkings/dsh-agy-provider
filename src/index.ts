/**
 * Project scaffold only.
 *
 * The actual DSH plugin registration is intentionally deferred until the
 * target DSH version and its provider contract are verified in milestone M1.
 */
export const packageName = "dsh-agy-provider";

export const scaffoldStatus = "dsh-provider-contract-pending" as const;

